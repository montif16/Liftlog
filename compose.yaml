services:
  postgres:
    image: 'postgres:16'
    container_name: liftlog-postgres
    environment:
      - 'POSTGRES_DB=liftlog'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
